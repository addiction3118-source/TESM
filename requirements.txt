# BlackArachnia — зависимости проекта (точные версии для воспроизводимости)
# Установка:  pip install -r requirements.txt
#
# Версии зафиксированы (pin) специально: раньше run.py ставил latest,
# и сборка была невоспроизводимой. Эти версии проверены и работают.

streamlit==1.57.0
groq==1.2.0
google-generativeai==0.8.6
openai==2.38.0
python-dotenv==1.2.2
psutil==7.2.2
cryptography==48.0.0
keyring==25.7.0
